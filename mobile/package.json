{
  "name": "strawverse-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "StrawVerse Android app (Capacitor shell around the shared backend + GUI)",
  "scripts": {
    "sync:android": "cd ../src/gui && npm run build && cd ../../mobile && node scripts/sync.mjs && cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/browser": "^6.0.4",
    "@capacitor/core": "^6.2.0",
    "capacitor-nodejs": "https://github.com/hampoelz/capacitor-nodejs/releases/download/v1.0.0-beta.8/capacitor-nodejs.tgz"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "esbuild": "^0.25.0"
  }
}
